---

- hosts: all
  become: true
  tasks:
  

  - name: uninstall apache2 package
    apt:
      name: apache2
      state: absent

  - name: remove hp support for apache
    apt:
      name: libapache2-mod-php
      state: absent
